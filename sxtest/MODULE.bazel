"""sxtest - Headless Snes9x test harness for SNES ROMs"""

module(
    name = "sxtest",
    version = "1.0.0",
)

bazel_dep(name = "googletest", version = "1.14.0.bcr.1")
bazel_dep(name = "rules_cc", version = "0.0.17")
bazel_dep(name = "platforms", version = "0.0.10")
